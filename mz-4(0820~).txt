0820 =================================================================
EKS 

기본 환경 구성
awscli / kubectl / cksctl 설치

aws configure 을 통해 입력된 값은 .aws 내부의 파일 두개에 저장된다.

# k8s
# 설치 파일
# 특정 버전 다운로드 : curl -LO "https://dl.k8s.io/release/v1.34.9/bin/linux/amd64/kubectl"

curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
# 실행권한을 줘야 한다.
chmod +x kubectl
# 사용자가 일반적으로 명령어를 실행하는 디렉토리로 옮긴다.
sudo mv kubectl /usr/local/bin
# 실행 후 버전 확인
kubectl version


스웜 기준으로 매니저는 마스터이고,
워커는 노드그룹으로이루어 진다.
이러한 노드 그룹은 각각이 오토스케일 그룹을 형성한다.
그리고 필요한 이미지는 스케쥴러 입력되어 실행되는 형식이다.
말이 워커지 노드 그룹은 노드그룹이고, 스케쥴은 노드 하나 단위.

# eks 생성
eksctl create cluster --name std20-eks-cluster --region ap-east-1 \
--nodegroup-name std20-ng-t3 --node-type t3.micro \
--nodes 2 --nodes-min 1 --nodes-max 3



# 노드 그룹 확인
eksctl get nodegroup --cluster std20-eks-cluster


보통 eks 를 생성하면, 새로운 vpc를 생성한다.
하지만, 기존에 있는 vpc를 활용할 경우,
eks가 서브넷에 원활히 접근 가능하도록 태그값을 붙일 필요가 있다.

TAG 설정
pub:
kubernetes.io/role/elb = 1
kubernetes.io/cluster/[클러스터 이름] = shared


pri:
kubernetes.io/role/internal-elb = 1
kubernetes.io/cluster/[클러스터 이름] = shared





이렇게 사용중인 클러스터에서 노드 확인이 가능하고, 컨테이너를 배포한다면, 이렇게 나온 노드들에 배포된다.

kubectl get node


# 실제로는 잘 쓰지 않지만 한번씩 쓸 일이 생기는 코드
# 클러스터 목록 확인 : 
	kubectl config get-contexts
# 현재 사용중인 클러스터
	kubectl config current-context
# 클러스터 선택
	kubectl config use-context <사용할 클러스터 이름>
# 사용 클러스터 등록
	aws eks update-kubeconfig --region <리전> --name <클러스터 이름>
	
	
ecr 레포지토리 만들기

aws ecr get-login-password --region <리전명> | docker login --username AWS --password-stdin <aws account>




eks에서 지원하는 이미지인지 확인하는 방법

aws ssm get-parameter \
  --name /aws/service/eks/optimized-ami/1.30/amazon-linux-2023/x86_64/standard/recommended/image_id \
  --region ap-east-1 \
  --query "Parameter.Value" \
  --output text
  
  
# 레포지토리 로그인
aws ecr get-login-password --region ap-south-1 | \
    docker login --username AWS \
    --password-stdin 626635419731.dkr.ecr.ap-south-1.amazonaws.com

# 보안 스캔이 포함된 레포지토리 생성 (이미지의 취약점을 자동으로 체크)
aws ecr create-repository \
    --repository-name std20/ex-repo \
    --region ap-east-1 \
    --image-scanning-configuration scanOnPush=true \ # 추가 : 취약점 자동 검사
    --encryption-configuration encryptionType=AES256 # 저장된 이미지 암호화

# 레포지토리 목록 확인
aws ecr describe-repositories --region ap-south-1

# 레포지토리 목록만 표로 출력
aws ecr describe-repositories --query 'repositories[].repositoryName' --output table


# 이미지 목록만 간략히 보기
aws ecr describe-images \
  --repository-name ian/nginx \
  --region ap-south-1 \
  --query 'sort_by(imageDetails, &imagePushedAt)[*].{Tag:imageTags[0], PushedAt:imagePushedAt}' \
  --output table
# 이미지 빌드(생성)
# 이미지 태그생성
docker tag csjin/nginx:latest 626635419731.dkr.ecr.ap-northeast-1.amazonaws.com/csjin/nginx:latest

# 이미지 푸시(올리기)
docker push 626635419731.dkr.ecr.ap-northeast-1.amazonaws.com/csjin/nginx:latest

# 이미지 삭제
aws ecr batch-delete-image \
    --region ap-northeast-1 \
    --repository-name csjin/nginx \
    --image-ids imageTag=latest-aws
    
# 이미지가 없는 빈 리포지토리 삭제
aws ecr delete-repository --repository-name csjin/nginx --region ap-northeast-1

# [주의] 이미지가 들어있는 리포지토리 강제 삭제 (--force)
aws ecr delete-repository --repository-name csjin/nginx --region ap-northeast-1 --force  
  
  
# ============================================================================
0820 -
오전 - 기본적인 명령어

오후 - manifast 를 활용한 인프라 구성.






=============================================================================
0824

파드 실행 명령어 :  kubectl apply -f <파일 이름>

파드 실행 되고 있는지 확인 : kubectl get pods -o wide

파드 삭제 명령어 : kubectl delete -f <파일 이름>


파드를 여러개를 띄우면서 리소스를 지정하는데,
Request 값과 Limit 값으로 나뉜다.
이 두값의 차이로 QoS 등급이 나뉘는데,

개런티
버스터드
베스트 에폴트
로 나뉜다.

이 나뉘는걸 통해,

pod 내 환경 변수 설정하기
pod-env-configmap

configmap의 경우 env 내에서 값을 가져올 config 맵의 이름을 같은 이름으로 지정해주어야 하는데, 이 경우 보통 Pod 위쪽에 위치한다.


secret

인코딩
read -s -p "Enter MYSQL Root Password: " password && printf "%s" "$password" | base64 && echo


출력시에는 디코딩이 되어서 출력된다.


볼륨의 종류 : 
	- empty: 임시 저장소를 사용하는 볼륨, 주로 컨테이너 간의 자료 공유
	- hostPath: 노드의 디렉토리와 컨테이너의 디렉토리를 연결
		(중요) 모든 워커노드에 동일한 경로의 디렉토리를 생성해 줘야됨.
	- PV & PVC : ec2 인스턴드 또는 efs를 사용하는 볼륨으로 공간 할당을 우선 만들어 놓고, 할당하는 방식
	- configMap / Secret : 일반적으로 환경설정 파일을 생성하거나 수정해 줘야 할때.
	- NFS : 네트워크 저장소를 볼륨으로 사용(AWS 에서는 efs를 사용)
	

PV & PVC 
	PV 는 namespace 에 포함되지 않는다.
	namespace 단위 관리에 포함되지 않기에, 일괄 삭제가 되지 않는다. 따라서 신중한 관리가 필요하다.
	
	
	
통상적인 구조

	
---
# 1. Config / Secret / PV / PVC

# 1.1 PV

# 1.2 Namespace

# 1.3 PVC / ConfigMap / Secret 

# 1.4 Volume / VolumeMount

---
# 2. POD / Deployment(APP) / StatefulSet(DB) / DaemonSet(특수 목적)

---
# 3. SVC


# 08.24 
볼륨 실습
1. MySQL 설치 (8.0)
2. 초기화 파일을 이용하여 사용자 std00와 데이터베이스(testdb) 생성
3. svc 를 이용하여 파드가 재생성되어도 연결할 수 있도록 구성한다.
4. 데이터베이스 파일은 로컬에 저장.


===============================================================================
0825

deployment

pod를 관리하기 위한 도구
라벨의 키 벨류값을 기준으로 셀렉터를 이용 파드를 선택한다.

무언가 안되거나 버그가 발생하였을때,
kubectl describe <~~~> 
를 활용한다.
때때로 로그를 볼때보다 더 효과가 좋다.


컨테이너 리소스 정의
	어제의 연장선 겸 복습 : 주 컨테이너는 리소스를 정의 하는것을 추천한다.
	
	
# Deployment 관련 주요 명령어 ---------------------------------------------------
# -----------------------
# 롤백(Rollback)
# -----------------------
# kubectl rollout undo deployment/<deployment_name>

# -----------------------
# 배포 진행상태 확인
# -----------------------
# kubectl rollout status deployment/<deployment_name>

# -----------------------
# 파드 갯수 동적 조정
# -----------------------
# kubectl scale deployment/<deployment_name> --replicas=<number_of_replicas>
	
	
[std20-2-tier-web.yaml]	
1. nginx 파드 최소 1, 최대 3
2. 로드밸런서로 외부접속이 가능해야함.(도메인 연결)
3. ConfigMap을 이용한 index.html변경
4. ConfigMap을 이용한 Nginx의 환경설정 파일 default.conf 변경
5. fastAPI 는 jinja2로 구성된 페이지 로드: http://개인도메인//api
6. fastapi 최소 2, 최대 6
7. index.html에서 특정 버튼(링크)을 클릭하면 fastAPI의 jinja2 페이지가 뜨도록 구성


====================================================
08.25
statefulset

일반적인 오류시 라벨 기반 추적 로그 30줄
kubectl logs -l app=<라벨 이름> --tail=30
